# Application Name
spring.application.name=taskmanager

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/taskmanager
spring.datasource.username=postgres
spring.datasource.password=kratos
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=create  
spring.jpa.show-sql=true

# Disable Spring Security login (temporary)
spring.security.user.name=disabled
spring.security.user.password=disabled

server.port=8082

# Logging
logging.level.org.springframework=ERROR

# JWT
spring.app.jwtSecret=mylongsecretkey123dgrgdrggdgdgrrdgdgrgdrgdgrg
spring.app.jwtExpirationMs=86400000

# Actuator
management.endpoints.web.exposure.include=* 
management.endpoint.health.show-details=always
management.info.env.enabled=true
management.endpoint.shutdown.enabled=true
server.shutdown=graceful

# App Info
info.app.name=TaskManager
info.app.description=TaskManager Fullstack app
info.app.version=1.0.1
